#include <iostream>
#include <string>
using namespace std;
// Selection algorithm
// Using selection algorithm order alphabetically of list
int main(void)
{
    string userNameList[5] = {"kailash", "aryan", "shyam", "mohan", "laxmi"};
    for (int i = 0; i < 5; i++)
    {
        cout << userNameList[i] << endl;
    }

    cout << endl;
    cout << endl;

    for (int i = 0; i < userNameList->length(); i++)
    {
        for (int j = 0; j < userNameList->length(); j++)
        {
        }
    }
}